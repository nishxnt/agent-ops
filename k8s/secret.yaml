# WARNING: placeholder values only. For real deployments use:
#   kubectl create secret generic agentops-secrets \
#     --from-env-file=.env --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: agentops-secrets
  labels:
    app.kubernetes.io/name: agentops
type: Opaque
stringData:
  GROQ_API_KEY: "change-me"
  LANGSMITH_API_KEY: ""
