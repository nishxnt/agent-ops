# Phase 3 Demo Artifacts

These files capture one real run of the AgentOps pipeline in MOCK mode, frozen at Phase 3 completion. They demonstrate:

- Pipeline orchestration end-to-end (DONE status with PASS decision)
- Per-agent cost attribution (planner, researcher, writer)
- Hash-chained audit trail (verify_chain returns True)
- Structured ResearchReport output

Regenerate with: `uv run python examples/generate_example.py`
